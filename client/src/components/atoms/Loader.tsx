import React from "react";

const Loader = () => {
  return (
    <div
      className={`animate-spin w-8 h-8 rounded-full border-4 border-blue-500 border-t-transparent`}
    />
  );
};

export { Loader };
